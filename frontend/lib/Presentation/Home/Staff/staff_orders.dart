import 'package:flutter/material.dart';

class StaffOrderSection extends StatelessWidget {
 const StaffOrderSection({super.key});

  @override
  Widget build(BuildContext context) {
    return SafeArea(
      child: Text('Order'),
    );
  }
}